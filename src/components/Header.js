import React from "react";

function Header(props) {
    return (
        <header>
            <h1>ToDo Application</h1>
            <p>Management of ToDo's</p>
        </header>
    );
}

export default Header;